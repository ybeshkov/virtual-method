#include "Octal.h"
#include <iostream>

Octal::Octal() : Array() {}

Octal::Octal(int n) : Array(n) {}

Array Octal::operator+(Array& in_arr) {
    array min(min);
    for (int i = 0; i < 5; ++i)
    {
        if (m[Array] < min)
        {
            min = m[Array];
        }
    }
	return min;
}
